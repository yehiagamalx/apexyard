# ARI Publication Importer — C4 Level 2 (Container Diagram)

> **Note**: this diagram was auto-generated by /handover on 2026-04-28 from repo signals (plugin file structure, class files, CHANGELOG). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector may have picked a framework version wrong
> - Inferred relationships — Editor → WordPress Admin assumes HTTPS; adjust if your stack uses something else
> - External systems — anything your team uses that isn't in PHP files (e.g. CDN, S3) won't have been detected
>
> Update the "Maintenance" section below once the diagram is stable.

```mermaid
C4Container
  title ARI Publication Importer — Container Diagram

  Person(editor, "ARI Editor", "Uploads Word/RTF documents and reviews before publishing")

  System_Boundary(wordpress, "WordPress (Arab Reform Initiative)") {
    Container(admin_ui, "Import Admin UI", "PHP + jQuery", "Upload form and review screen — rendered server-side, interactivity via AJAX")
    Container(plugin, "ARI Importer Plugin", "PHP 7.4+", "Handles file upload, docx parsing, author/tag matching, draft creation. Integrates with ACF Pro and WPML.")
    ContainerDb(db, "WordPress Database", "MySQL", "Stores publications, people CPT, post_tag terms, wp_options (API key, Pandoc path)")
  }

  System_Ext(pandoc, "Pandoc", "External binary on server. Converts .docx / .rtf to HTML via exec().")
  System_Ext(claude, "Anthropic Claude API", "LLM API. Mode 2 smart detection — author matching and tag suggestion.")

  Rel(editor, admin_ui, "Uploads document, reviews results", "HTTPS")
  Rel(admin_ui, plugin, "AJAX requests (upload, process, create draft)", "wp-admin/admin-ajax.php")
  Rel(plugin, db, "Reads people CPT, post_tag terms; writes draft publication", "WordPress APIs / ACF")
  Rel(plugin, pandoc, "Converts uploaded file to HTML", "exec() / shell")
  Rel(plugin, claude, "Sends document + author/tag lists for smart detection (Mode 2)", "HTTPS / REST API")
```

## Maintenance

(Handover-generated — update when containers change, e.g. if Pandoc is replaced with a PHP library, or if the plugin is split into separate services.)
