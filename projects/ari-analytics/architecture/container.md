# Container Diagram — ARI Analytics

> **C4 Level 2** — the system broken down into deployable/runnable containers. Audience: the dev team.

## Diagram

```mermaid
C4Container
    title Container Diagram for ARI Analytics

    Person(user, "ARI Staff", "Authenticated user viewing analytics")

    System_Boundary(boundary, "ARI Analytics") {
        Container(nginx, "Nginx Reverse Proxy", "nginx:alpine", "SSL termination, routes /api/* to backend, all other traffic through OAuth2 proxy")
        Container(oauth2proxy, "Auth Gate", "oauth2-proxy v7.6", "Enforces Google login; injects X-Forwarded-Email header")
        Container(web, "Dashboard", "React 19 / Vite / Nginx", "Multi-page analytics UI: site overview, publications, events, authors, newsletter, report builder")
        Container(api, "Admin API", "Python / FastAPI", "Data health, page visibility config, pipeline triggers, user activity logs")
        ContainerDb(db, "Data Store", "SQLite + data.json", "Pipeline writes SQLite; export_json.py serialises to data.json consumed by dashboard")
        Container(pipeline, "Data Pipeline", "Python scripts (host cron)", "Pulls GA4, GSC, Zoom, Mailjet, WordPress; writes to SQLite")
    }

    System_Ext(ga4, "Google Analytics 4", "Page views, sessions, traffic sources")
    System_Ext(gsc, "Google Search Console", "Search impressions, clicks")
    System_Ext(zoom, "Zoom API", "Webinar registrant and attendance data")
    System_Ext(mailjet, "Mailjet", "Newsletter campaign statistics")
    System_Ext(wp, "WordPress REST API", "Publication metadata, authors, programs, CPTs")
    System_Ext(googleauth, "Google OAuth2", "Identity provider for staff login")

    Rel(user, nginx, "Visits", "HTTPS")
    Rel(nginx, oauth2proxy, "Unauthenticated traffic", "HTTP")
    Rel(oauth2proxy, googleauth, "Redirects for login", "OIDC")
    Rel(oauth2proxy, web, "Authenticated traffic", "HTTP")
    Rel(nginx, api, "Proxies /api/*", "HTTP / JSON")
    Rel(web, api, "Calls admin endpoints", "HTTPS / JSON")
    Rel(api, db, "Reads data.json, reads/writes config.json", "File I/O")
    Rel(pipeline, ga4, "Pulls analytics data", "Google Analytics Data API")
    Rel(pipeline, gsc, "Pulls search data", "Google Search Console API")
    Rel(pipeline, zoom, "Pulls webinar data", "Zoom REST API")
    Rel(pipeline, mailjet, "Pulls campaign data", "Mailjet REST API")
    Rel(pipeline, wp, "Pulls content metadata", "WordPress REST API")
    Rel(pipeline, db, "Writes analytics data", "SQLite + JSON export")
    Rel(api, pipeline, "Triggers refresh via refresh.sh", "subprocess")
```

> **Note**: this diagram was auto-generated by /handover on 2026-05-02 from repo signals (docker-compose.yml, package.json, Python requirements, pipeline scripts). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector may have picked a framework version wrong
> - Inferred relationships — user → nginx assumes HTTPS; adjust if your stack uses something else
> - External systems — anything your team uses that isn't in package.json (e.g. infra-only dependencies, direct cloud APIs called via HTTP) won't have been detected
>
> Update the "Maintenance" section below once the diagram is stable.

## Maintenance

(From the template — update when L2 containers change.)
